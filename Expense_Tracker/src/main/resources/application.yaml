spring:
    datasource:
        url: jdbc:mysql://localhost:3306/expense_tracker?useSSL=false&allowPublicKeyRetrieval=true
        username: root
        password: root

spring.jpa.hibernate.ddl-auto: update
